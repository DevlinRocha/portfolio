{
    "extends": "../../tsconfig.base.json",
    "compilerOptions": {
        "useDefineForClassFields": true,
        "lib": ["ESNext", "DOM", "DOM.Iterable"],

        /* Bundler mode */
        "jsx": "react-jsx",

        /* Linting */
        "allowJs": false,

        /** Path Aliasing */
        "paths": {
            "@/*": ["./src/*"]
        }
    },
    "include": ["src"]
}
